{{#if description}}
/**
 * {{{getComment description}}}
 */
{{/if}}
export enum {{{name}}} {
    {{#each enum}}
    {{#if description}}
    /**
     * {{{getComment description}}}
     */
    {{/if}}
    {{#containsSpaces name}}
    "{{{name}}}" = {{{value}}},
    {{else}}
    {{{name}}} = {{{value}}},
    {{/containsSpaces}}
    {{/each}}
}
